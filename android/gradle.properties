org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Pin the Gradle JVM to JDK 17 (LTS).
# This machine's default JDK is 25, and the Kotlin DSL script compiler bundled
# with Gradle cannot parse the "25.0.2" version string — every `flutter run`
# dies with a bare `* What went wrong: 25.0.2` whenever the .kts build scripts
# need recompiling (i.e. any time a --dart-define changes the cache key).
# Plain `gradlew assembleDebug` can appear to work off a warm cache, which makes
# this look intermittent. Remove this line only once the toolchain supports JDK 25.
org.gradle.java.home=/Users/tejastamkar/.sdkman/candidates/java/17.0.16-ms

android.useAndroidX=true
android.enableJetifier=true
android.newDsl=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
